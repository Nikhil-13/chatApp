import {StyleSheet} from 'react-native';
export const styles = StyleSheet.create({
  rootContainer: {
    padding: 10,
    maxWidth: '75%',
    margin: 10,
    borderRadius: 10,
  },
  chatText: {
    color: 'white',
  },
});
